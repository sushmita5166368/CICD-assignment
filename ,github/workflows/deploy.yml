name: CI-CD Pipeline

on:
  push:
    branches:
          -main

jobs:
  test:
    runs-on : ubuntu-latest

    steps:
      -name: Checkout code
       use  : actions/checkout@v4

       -name: Check files
       run: |
           ls -la
